{
  "name": "bonus-buy-widget",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "OBS overlay for streamers running bonus-buy sessions: live buy-in, multipliers, and break-even",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js"
  },
  "engines": {
    "node": ">=18"
  }
}
